function loadHistory(){const e=JSON.parse(localStorage.getItem("sleepSessions")||"[]"),n=document.getElementById("historyList");n&&(0!==e.length?(e.sort((e,n)=>new Date(n.date)-new Date(e.date)),n.innerHTML=e.map(e=>{const n=new Date(e.date).toLocaleDateString("en-US",{weekday:"long",year:"numeric",month:"long",day:"numeric"}),s=e.log.length>0?`${e.log.length} checks performed`:"No checks",t=e.log.map(e=>`<div class="log-entry">\n        <span class="time">${e.time}</span> - ${e.message}\n      </div>`).join("");return`\n      <div class="history-item">\n        <div class="history-date">${n} - Day ${e.day}</div>\n        <div class="history-duration">${s}</div>\n        <div class="session-log margin-top">${t}</div>\n      </div>\n    `}).join("")):n.innerHTML="<p>No sessions saved yet.</p>")}